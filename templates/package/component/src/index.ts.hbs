/**
 * Main entry point - Web only (no react-native dependency)
 * For React Native, import from '@gp/poster/native'
 */

/** Dependencies */
import { default as {{pascalCase componentName}} } from './{{componentName}}';

/** Export types */
export type { {{pascalCase componentName}}Props } from './{{componentName}}';

/** Export hooks */
export { use{{pascalCase componentName}} } from './use-{{componentName}}';

/** Export component */
export { {{pascalCase componentName}} };

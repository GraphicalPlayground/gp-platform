// Copyright (c) - Graphical Playground. All rights reserved.
// For more information, see https://graphical-playground/legal
// mailto:support AT graphical-playground DOT com

import React from 'react';
import clsx from 'clsx';
import styles from './{{kebabCase name}}.module.css';

/**
 * @brief {{pascalCase name}} component props interface.
 * @see {{pascalCase name}}
 */
export interface {{pascalCase name}}Props extends React.HTMLAttributes<HTMLElement> {}

/**
 * @brief {{pascalCase name}} component.
 * @details
 */
export const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = ({className, ...rest}) => {
  return <div className={clsx(styles['{{kebabCase name}}'], className)} {...rest} />;
};

/** Set the display name for the {{pascalCase name}} component. */
{{pascalCase name}}.displayName = 'gp.{{pascalCase name}}';

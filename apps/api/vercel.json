{
  "version": 2,
  "framework": null,
  "buildCommand": "turbo run build --filter=@gp/api",
  "installCommand": "npm install -g pnpm@10.22.0 && pnpm install",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}
